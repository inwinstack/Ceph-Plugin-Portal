'use strict';

describe('GUI-Demo.InstallCtrl module', function() {

  beforeEach(module('GUI-Demo.InstallCtrl'));

  describe('InstallCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var InstallCtrl = $controller('InstallCtrl');
      expect(InstallCtrl).toBeDefined();
    }));

  });
});